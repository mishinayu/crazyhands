[server]
hostname = "{{ ansible_hostname }}"
node_ip = "127.0.0.1"
base_path = "https://$ref{server.hostname}:${carbon.management.port}"

[super_admin]
username = "admin"
password = "admin"
create_admin_account = true

[user_store]
type = "read_write_ldap_unique_id"
connection_url = "ldap://localhost:${Ports.EmbeddedLDAP.LDAPServerPort}"
connection_name = "uid=admin,ou=system"
connection_password = "admin"
base_dn = "dc=wso2,dc=org"      #refers the base dn on which the user and group search bases will be generated

[database.identity_db]
type = "postgre"
#url = "jdbc:postgresql://{{ db_node }}:5432/WSO2IDENTITY_DB;DB_CLOSE_ON_EXIT=FALSE;LOCK_TIMEOUT=60000"
hostname = "{{ db_node }}"
name = "{{ wso_identity_db }}"
port = "{{ db_port }}"
username = "{{ wso2is_database_user }}"
password = "{{ wso2is_database_pass }}"

[database.shared_db]
type = "postgre"
hostname = "{{ db_node }}"
name = "{{ wso_shared_db }}"
port = "{{ db_port }}"
#url = "jdbc:postgresql://{{ db_node }}:5432/WSO2SHARED_DB;DB_CLOSE_ON_EXIT=FALSE;LOCK_TIMEOUT=60000"
username = "{{ wso2is_database_user }}"
password = "{{ wso2is_database_pass }}"

#[keystore.primary]
#file_name = "wso2carbon.jks"
#password = "wso2carbon"

